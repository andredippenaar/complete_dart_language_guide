void main() {
  //ititialisation
  String title = 'Dart Course';
  print(title);
  //assignment - taking a variable that is already declared and assigning a new value to it
  title = title.toUpperCase();
  print(title);
}
